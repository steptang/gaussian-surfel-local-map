*.pyc
.vscode
output
build
# Top-level data/ only -- intended for scene inputs (e.g., DTU scans).
# Without the leading slash this matched 'tracking/data/' as source code
# was silently dropped from commits. See commit history if you need the
# back-story.
/data
*.ply
**/PKG-INFO
# submodules
**__pycache__**
*.png
*.out
eval
*.npz
**/tmp